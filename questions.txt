(1) Filter 2 is ~5x faster than filter 1. Similar to the example in Table 1 in Dive into Systems section 11.3 Locality, the difference between filters 1 and 2 has to do with cache memory access. Filter 2 benefits from spatial locality, and therefore can more quickly access cache lines that already are loaded. Filter one has to make much larger jumps between lines and has to make more calls to memory which is slower than accessing data from a cache. 